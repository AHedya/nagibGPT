[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "nagibGPT_frontend"
requires-python = ">=3.10,<3.11"
version = "0.0.1"
dependencies = [
    "requests",
    "streamlit"
]

[tool.setuptools.packages.find]
where = ["src"]